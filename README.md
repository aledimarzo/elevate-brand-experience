
  # Sito web per MIRAGE RP

  This is a code bundle for Sito web per MIRAGE RP. The original project is available at https://www.figma.com/design/7q9RsDaOKD2wEm6m3ywmji/Sito-web-per-MIRAGE-RP.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  